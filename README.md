# pharmacy-database

# pharmacymanager zip includes all java spring boot (back end) with maven
# react used for front end with file contained
# database folder has all database schema + data included
